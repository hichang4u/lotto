{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev --show-interactive-dev-session=false --port=8787",
    "deploy": "node scripts/deploy.mjs",
    "init-db": "wrangler d1 execute lotto_db --local --file=./schema.sql",
    "init-db:remote": "wrangler d1 execute lotto_db --remote --file=./schema.sql",
    "seed": "node scripts/seed.js",
    "seed:apply": "wrangler d1 execute lotto_db --remote --file=./seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260124.0",
    "hono": "^4.11.6"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.60.0"
  }
}
